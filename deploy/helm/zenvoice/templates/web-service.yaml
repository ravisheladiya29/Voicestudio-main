apiVersion: v1
kind: Service
metadata:
  name: {{ include "zenvoice.web.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "zenvoice.labels" . | nindent 4 }}
    app.kubernetes.io/component: web
  {{- with .Values.web.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.web.service.type }}
  ports:
    - name: http
      port: {{ .Values.web.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "zenvoice.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: web
