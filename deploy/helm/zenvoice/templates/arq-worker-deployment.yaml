apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "zenvoice.arqWorker.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "zenvoice.labels" . | nindent 4 }}
    app.kubernetes.io/component: arq-worker
spec:
  replicas: {{ .Values.workers.replicaCount }}
  selector:
    matchLabels:
      {{- include "zenvoice.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: arq-worker
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        {{- include "zenvoice.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: arq-worker
      {{- with .Values.workers.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      serviceAccountName: {{ include "zenvoice.serviceAccountName" . }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: arq-worker
          image: {{ include "zenvoice.image" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ["./scripts/run_arq_worker.sh"]
          envFrom:
            {{- include "zenvoice.backendEnvFrom" . | nindent 12 }}
          env:
            {{- include "zenvoice.dbEnv" . | nindent 12 }}
          livenessProbe:
            {{- toYaml .Values.workers.livenessProbe | nindent 12 }}
          resources:
            {{- toYaml .Values.workers.resources | nindent 12 }}
      {{- with .Values.workers.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.workers.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.workers.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
